export const DEFAULT_SES_SENDER_FALLBACK = 'no-reply@example.com';
export const SNS_PUBLISH_TIMEOUT_MS = 5000; // Example timeout, might not be used directly by SDK
export const MAX_SNS_SUBJECT_LENGTH = 100; // Max subject length for SNS
export const SES_MESSAGE_ID_HEADER = 'X-SES-Message-ID'; // Common header name for SES message ID