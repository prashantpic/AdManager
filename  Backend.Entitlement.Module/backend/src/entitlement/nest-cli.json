{
  "$schema": "https://json.schemastore.org/nest-cli",
  "collection": "@nestjs/schematics",
  "sourceRoot": "src",
  "compilerOptions": {
    "deleteOutDir": true,
    "webpack": false, // Set to true if using webpack, typically false for libraries
    "tsConfigPath": "tsconfig.build.json" // Assumes a tsconfig.build.json for build-specific options
  }
}